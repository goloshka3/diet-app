# このプロジェクトについて

ダイエット用の食事記録アプリを、**プログラミング初心者が作りながら学ぶ**プロジェクト。

## 利用者

- AI・プログラミング初心者。専門用語は初出時に短く説明する。前提知識を仮定しない。
- 環境は Windows 11 / PowerShell。返答は日本語。
- スマホ（iPhone）で使うことを想定。開発は PC。

## 進め方

- 全体計画は `../AIの作業場/ダイエットアプリ_制作ロードマップ.md`。ステージを1つずつ完成させる。
- **一度に大きく作らない。** 小さいステップに分け、その都度どう動くか説明する。
- コードを書いたら「何をしているか」「なぜそう書くか」を必ず添える。
- ユーザーが理解しないまま先に進まない。分からない箇所は止まって説明する。
- 1つ機能ができたら `git commit`。コミットメッセージは日本語で簡潔に。

## 技術方針

- HTML + CSS + JavaScript（素のまま。フレームワーク・ビルド・npm は使わない）。
- データ保存はブラウザの `localStorage`。
- 公開は GitHub Pages（静的サイト）。
- この方針はステージ3くらいまで。超える必要が出たら、その時に相談して決める。

## やらないこと（今は）

- フレームワーク（React 等）の導入
- サーバー・データベース
- Claude API 連携（ステージ5で検討）
